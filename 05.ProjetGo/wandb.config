WANDB_CONFIG = {

    "entity": "malik-chettih-ia-dauphine-university-in-paris-org",  # Votre nom d utilisateur WandB
    "project_name": "go-mobilenet-research",  # Nom de votre projet

    "default_tags": ["go", "mobilenet", "research", "votre-lab"],

    "log_frequency": 1,              # Log chaque époque
    "save_model_frequency": 20,      # Sauvegarder modèle tous les 20 epochs
    "log_weights": True,             # True si vous voulez voir les histogrammes de poids
    "log_gradients": False,          # True pour debugging avancé


    "slack_webhook": None,           # URL webhook Slack si vous en avez un
    "email_alerts": None,            # Email pour alertes (non implémenté)


    "alert_thresholds": {
        "min_policy_accuracy": 0.30,    # Alerte si accuracy < 30%
        "max_value_mse": 0.50,          # Alerte si MSE > 0.50
        "max_loss_explosion": 5.0,      # Alerte si loss > 5x baseline
    }
}